# Vibrise 产品逻辑文档

> 从 HTML 原型中提取的核心业务逻辑，供 App 开发参考。  
> 这些逻辑经过原型验证，可以直接移植到 Flutter/React Native 等框架。

---

## 一、闹钟数据模型

```json
{
  "id": 1,
  "enabled": true,
  "hour": 7,
  "minute": 30,
  "days": [1, 2, 3, 4, 5]
}
```

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | int | 自增唯一标识 |
| `enabled` | bool | 是否启用 |
| `hour` | int | 0-23 |
| `minute` | int | 0, 5, 10, ..., 55（5分钟步进） |
| `days` | int[] | 重复日，0=Sun, 1=Mon, ..., 6=Sat；空数组=一次性 |

**持久化**：localStorage（原型）→ App 用 SQLite / SharedPreferences

---

## 二、音乐选择策略

### 双维度模型（当前产品真相）

Vibrise 当前定稿的选歌策略不是“5 个维度里随机挑一个”，而是固定使用两个维度同时生效：

| 维度 | Jamendo 参数 | 作用 |
|------|--------------|------|
| weather（天气） | `fuzzytags` | 决定音乐的情绪 / 氛围 |
| time（时间） | `speed` | 决定音乐的节奏快慢 |

### 选歌流程

```
1. 获取位置、天气、当前时间
2. 天气 -> fuzzytags
3. 时间 -> speed
4. 组合 { fuzzytags, speed } 请求 Jamendo
5. 如果候选池太小（< 100）:
     先去掉 speed，仅保留天气 tag 重试
6. 如果仍为空:
     纯随机取歌
7. 如果 API 失败:
     使用 Demo 歌曲兜底
```

### 为什么不再使用 v1 的五维随机模型

- 天气、温度、星期、时间、随机五个维度共用一个 `fuzzytags` 入口，彼此冲突太多
- 小曲库 tag 很容易导致重复，甚至返回不适合闹钟场景的内容
- 双维度模型已经能提供“天气氛围 + 时间节奏”的足够变化，不需要再做维度抽签

---

## 三、天气代码 → 音乐标签映射

基于 [WMO Weather Code](https://open-meteo.com/en/docs) 标准。

| 天气代码 | 天气 | 音乐标签（fuzzytags） |
|---------|------|----------------------|
| 0-1 | 晴 / 大部晴 | happy |
| 2 | 多云 | lounge+downtempo |
| 3 | 阴天 | chillout |
| 45-48 | 雾 | atmospheric |
| 51-55 | 毛毛雨 | lofi+chillhop |
| 61-63 | 小雨 / 中雨 | jazz |
| 65 | 大雨 | cinematic |
| 71 | 小雪 | soft |
| 73-75 | 中雪 / 大雪 | newage |
| 80-82 | 阵雨 | jazz |
| 95+ | 雷暴 | epic+adventure |

说明：
- 天气维度负责“氛围感”，不再直接决定速度
- 如果天气码不在表内，按区间回退到最近的已知天气类别

---

## 四、已移除的旧维度

以下维度属于 v1 原型中的历史设计，不再作为 MVP 选歌输入：

| 维度 | 移除原因 |
|------|---------|
| temperature（温度） | 与天气高度重叠，且部分 tag 曲库太小 |
| weekday（星期） | 没有独立过滤轴，只会继续挤占 `fuzzytags` |
| random（随机维度） | 当前模型内部已经通过 `offset` 实现随机 |

这些维度未来如果回归，应作为“微调因子”而不是主筛选轴。

---

## 五、时间段 → 速度映射

| 时间段 | speed | 设计意图 |
|-------|-------|---------|
| 00:00-04:59 | verylow | 深夜 / 凌晨，极轻柔 |
| 05:00-06:59 | low | 清晨，温和渐进 |
| 07:00-10:59 | medium | 正常起床高峰，适中节奏 |
| 11:00-17:59 | high | 午休 / 下午，需要提神 |
| 18:00-20:59 | medium | 傍晚，回归平和 |
| 21:00+ | low | 夜间，避免过于激烈 |

说明：
- 时间维度只决定 `speed`
- 不再额外输出 tag，避免与天气维度重叠
- 默认不使用 `veryhigh`，因为整体偏刺激，不适合作为闹钟速度档

---

## 六、Jamendo 随机与回退策略

```
filter = { fuzzytags: weatherTag, speed: timeSpeed }

1. 先查 fullcount
2. 如果 fullcount >= 100:
     random offset in [0, min(fullcount, 500)]
3. 如果 fullcount < 100:
     去掉 speed，仅保留 fuzzytags 再查一次
4. 如果仍没有合适结果:
     纯随机取歌
5. 如果网络失败:
     Demo 歌曲兜底
```

这套回退顺序的目标是：
- 先尽量保留“天气 + 时间”语义
- 小池子时优先保住“天气氛围”
- 最后才退到完全随机

---

## 七、天气代码 → 视觉风格映射

### 天气 → 背景渐变色

| 代码 | 天气 | Emoji | 背景渐变末色 | 光球色1 | 光球色2 |
|------|------|-------|------------|--------|--------|
| 0 | Clear | ☀️ | #1a1810 | rgba(240,180,80,0.35) | rgba(255,200,100,0.15) |
| 1 | Mostly Clear | 🌤️ | #181814 | rgba(200,180,100,0.25) | rgba(180,160,100,0.15) |
| 2 | Partly Cloudy | ⛅ | #141418 | rgba(150,160,180,0.25) | rgba(120,130,160,0.15) |
| 3 | Overcast | ☁️ | #12121a | rgba(130,140,160,0.2) | rgba(100,110,130,0.15) |
| 45 | Foggy | 🌫️ | #101016 | rgba(120,120,140,0.2) | rgba(100,100,120,0.15) |
| 51 | Light Rain | 🌦️ | #101420 | rgba(100,130,200,0.25) | rgba(80,100,160,0.15) |
| 61 | Rain | 🌧️ | #0a1024 | rgba(70,100,180,0.3) | rgba(50,80,150,0.2) |
| 65 | Heavy Rain | 🌧️ | #080e22 | rgba(60,80,160,0.3) | rgba(50,60,130,0.2) |
| 71 | Light Snow | 🌨️ | #141620 | rgba(160,170,200,0.25) | rgba(140,150,180,0.15) |
| 73 | Snow | ❄️ | #141822 | rgba(170,180,210,0.3) | rgba(150,160,190,0.2) |
| 95 | Thunderstorm | ⛈️ | #100a22 | rgba(100,60,160,0.3) | rgba(60,40,120,0.2) |

### 温度 → 首页背景渐变色

| 温度范围 | 主色调 | 情绪 |
|---------|--------|------|
| ≥ 30°C | 橙红 (255,140,40) | 炎热 |
| 20-30°C | 暖黄 (240,180,80) | 温暖 |
| 10-20°C | 青蓝 (100,180,200) | 清爽 |
| 0-10°C | 冷蓝 (80,120,190) | 凉冷 |
| < 0°C | 灰蓝 (160,180,220) | 寒冷 |

---

## 八、时间 → 亮度曲线

页面整体亮度随时间变化，模拟自然光照周期。

### 公式

```
daylightFactor = 0.5 + 0.5 * cos((hour - 12) * π / 12)
```

| 时间 | daylightFactor | 效果 |
|------|---------------|------|
| 00:00（午夜） | 0.0 | 最暗 |
| 06:00（早晨） | 0.5 | 中等 |
| 12:00（正午） | 1.0 | 最亮 |
| 18:00（傍晚） | 0.5 | 中等 |

### 映射到 CSS 属性

| 属性 | 午夜值 | 正午值 |
|------|--------|--------|
| brightness | 0.50 | 1.20 |
| saturate | 1.10 | 1.50 |
| flow orb opacity | 0.35 | 0.70 |

---

## 九、Jamendo API 调用规范

### 基础配置

| 参数 | 值 |
|------|-----|
| base URL | `https://api.jamendo.com/v3.0/tracks/` |
| client_id | 需要注册获取 |
| format | json |
| vocalinstrumental | instrumental |
| audioformat | mp32 |
| imagesize | 400 |

### 随机选歌流程

```
1. 构造查询参数 { fuzzytags: tag, speed: speed }
2. 先查 fullcount（总数）
3. 随机生成 offset = random(0, min(fullcount, 500))
4. 用 offset 查询获取 1 首歌
5. 如果无结果 → 去掉标签限制，纯随机再取一首
6. 如果仍无结果 → 使用内置 demo 歌曲 + Web Audio fallback 旋律
```

### 音量渐入

- 初始音量 = 0
- 每秒增加 1/30（即 30 秒达到满音量）
- 同时适用于 Jamendo 音频和 Web Audio fallback 旋律

---

## 十、辅色点缀算法

首页天气背景上叠加 2-3 个随机辅色渐变，增加视觉丰富度。

### 辅色池

```json
[
  [255, 100, 150], [100, 255, 180], [255, 200, 50],
  [100, 200, 255], [200, 100, 255], [255, 150, 80],
  [80,  255, 200], [255, 80,  100], [150, 255, 100],
  [180, 120, 255], [255, 180, 200], [120, 220, 160]
]
```

### 算法

```
count = random(2, 3)
for each accent color:
  position = (random 10-90%, random 10-90%)
  opacity = random(0.05, 0.12)
  size = random from [20%, 30%, 45%, 60%, 75%]
  shape = random(ellipse, circle)
  → generate radial-gradient layer
prepend to weather background
```
